export namespace HeaderSampleModel{

    export interface UserData   {
        username : string;
        emailID : string;
        password : string;
      }
    
    export interface StudentList {
        name : string;
        age : number;
        }
    
    export interface ProductDetails{
            name: string;
            price: string;
            category: string;
            cover_image: string;
            description: string;
          }
}